# npm 安装时使用 legacy-peer-deps 模式
# 解决 NutUI-React-Taro 的 peer 依赖与项目 react-refresh 版本冲突
# （babel-preset-taro 要求 react-refresh ^0.14，项目用 ^0.18）
# 加这个文件后，任何人 clone 仓库都能顺畅 npm install，无需手动加 flag
legacy-peer-deps=true
